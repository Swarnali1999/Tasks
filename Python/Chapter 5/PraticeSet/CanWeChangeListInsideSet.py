'''
1. Can you change the values inside a list which is contained in set S.
   S = { 8, 7, 12, “Tiger”, [1, 2] }
   There can not be any list inside any set.

2. Can you change the values inside a tuple which is contained in set S.
   S = { 8, 7, 12, “Tiger”, (1, 2) }
   We can not change tuple (even if inside the set).

'''