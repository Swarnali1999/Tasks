# Set is a collection of non-repetitive elements.
# Sets are unordered(means elements order doesn’t matter),unindexed(means cannot access elements by index).
# There is no way to change items in sets and Sets cannot contain duplicate values.

a={1,'Hi',"2","hello"}
print(a)
print(type(a))
